import styled from 'styled-components';

const Title = styled.h2`
  color: #ff3163;
  margin-bottom: 1.5rem;
`;

const Tasks = () => (
  <div>
    <Title>Задания</Title>
  </div>
);

export default Tasks;
